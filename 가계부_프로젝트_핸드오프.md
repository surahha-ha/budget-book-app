# 가족 가계부 프로젝트 — 핸드오프 문서

> 작성일: 2026-06-23
> 목적: **다른 환경/세션(다른 Claude 계정)에서 이어서 작업**하기 위한 인수인계.
> 이 문서 하나로 맥락을 파악하고 웹앱 단계로 넘어갈 수 있도록 작성함.

---

## 0. 한눈에

- **무엇**: 가족 공동 가계부. 1단계로 **엑셀 가계부를 완성**했고, 2단계로 **웹앱 전환**을 계획 중.
- **현재 상태**: 엑셀 완성(디자인·기능·검증 끝). 웹앱은 **계획만 수립**(아직 코드 없음).
- **사용자 선호**: 한국어 응답 / 원화(₩) / 모던·파스텔 디자인 / 웹 앱 우선 / 단계적 진화.
- **다음 작업**: 웹앱 MVP (DB 스키마 → 화면 → 구현). 4장·5장 참고.

---

## 1. 프로젝트 개요 & 사용자 선호

| 항목 | 내용 |
|---|---|
| 사용 형태 | **가족 공동** 가계부 ("우리 살림" 기준, 구성원별 추적) |
| 통화 | 원화(₩) |
| 앱 1순위 플랫폼 | **웹 앱 우선** (반응형으로 모바일 겸용, 추후 PWA) |
| 디자인 취향 | 모던 미니멀 + **파스텔**, 넉넉한 여백, 격자선 최소화(카드형), 큰 글씨. 올드한 풀컬러 헤더·빽빽한 격자 거부. 빨강↔초록 조합("주식차트 같다") 싫어함. 포인트 색은 **연두 + 진노랑(밝은 톤)**. |
| 진행 방식 | 결정사항은 옵션 카드로 먼저 확인 → 실제 렌더(스크린샷) 보며 반복 조정 |

---

## 2. 완성된 산출물 — 엑셀 가계부

### 2-1. 파일 (모두 `C:\Users\Osstem\`)
- **`우리집_가계부.xlsx`** — 최종 결과물 (수식 6,300여 개, 오류 0개로 검증 완료)
- **`build_budget.py`** — 재생성 스크립트 (openpyxl). 디자인·항목·샘플 수정은 이 파일을 고쳐 재실행
- **`verify_and_preview.py`** — 검증/미리보기 스크립트 (Excel COM으로 재계산·오류검사 + 시트별 PNG 추출)

> 다른 PC에서 엑셀을 재생성하려면 `build_budget.py`를 함께 가져갈 것. 단순히 결과물만 쓸 거면 `우리집_가계부.xlsx`만 있으면 됨.

### 2-2. 시트 구성 (6개)
1. **시작하기** — 사용법(3단계), 색상 범례
2. **설정** — 기준 연/월, 구성원·결제수단·유형 목록, 지출 카테고리 14종 + 월 예산, (드롭다운용 통합 카테고리 목록 K열)
3. **거래내역** — 메인 입력. 날짜/유형/대분류/소분류/구성원/결제수단/금액/메모 + 자동 헬퍼열(연·월·주차·분기·연월). 1,200행에 수식·드롭다운 사전 적용
4. **대시보드** — 월간. KPI 6카드(총수입/총지출/소비지출/저축·투자/순잔액/저축률) + 카테고리 막대·구성원 파이·주차 라인 차트 + 예산 집행률 표(데이터바, 초과 빨강)
5. **기간리포트** — 주간(12주)·월간(12개월)·분기·연간 표 + 막대/라인 차트
6. **진단&인사이트** — 자동 진단(저축률·예산집행·예산초과·최대지출·외식비중·고정비 신호등 🟢🟡🔴 + 제안) + 한줄요약 + **정기지출 체크리스트**(고정비 자동 ☑완료/⬜미입력)

### 2-3. 디자인 시스템 (정확한 색상값 — 웹앱에서도 재사용 가능)

`build_budget.py` 상단 색상 상수. **HEX 그대로** 웹 CSS 변수로 옮겨 쓰면 톤 유지됨.

| 역할 | 변수 | HEX |
|---|---|---|
| 시트 제목 바 / 합계행 (연두) | NAVY | `8BC34A` |
| 차트 포인트 (밝은 진노랑) | TEAL / BRAND | `FFC72C` |
| 표 헤더 배경 (연한 연두) | HEAD_BG | `DCEDC8` |
| 헤더/제목 글씨 (다크 올리브) | HEAD_TX / SECTION | `33450F` |
| 본문 텍스트 | INK | `3A3330` |
| 보조 텍스트 | GRAY | `857F78` |
| 구분선 | LINE/BORDC | `EFE9DD` |
| 줄무늬/카드 배경 | ZEBRA/CARD | `FFFDF5` |
| 수입 (세이지) | INCOME | `7C9885` |
| 지출 (더스티 로즈) | EXPENSE | `CC8B96` |
| 저축 (더스티 블루) | SAVE | `7E9BBE` |
| 소비/데이터바 (골든) | AMBER | `E0A21E` |
| 저축률 (라벤더) | VIOLET | `9787BE` |
| 순잔액 (웜 그레이) | SLATE | `9A9286` |
| 입력칸 배경/글씨 | INPUTF / INPUTC | `FFEFB0` / `8A5A00` |
| 카드 틴트 | T_INCOME / T_EXPENSE / T_SAVE / T_AMBER / T_VIOLET | `EEF4EE` / `FAF0F2` / `EFF3F8` / `FFF4DA` / `F4F1F9` |

- **카테고리 막대 14색** (파스텔): `F2C14E, 7C9885, CC8B96, 7E9BBE, 9787BE, E0A21E, 8FBF9F, E8A87C, B5A8D1, 8CB8C4, C9A66B, D49AA6, 9DB17C, B0A18F`
- **구성원 파이 4색**: `FFC72C, 7E9BBE, 7C9885, CC8B96`
- 폰트: 맑은 고딕. 원칙: 격자선 제거(가로 구분선만), 카드형, 여백 넉넉, 타이포 위계(제목 크게).

### 2-4. 핵심 기능
- 입력은 `설정`+`거래내역`만, 나머지는 자동 계산.
- 저축률 = (수입 − 소비지출)/수입. 소비지출 = 총지출 − 저축/투자.
- 거래내역: 자동 필터·정렬, 수입행 연녹/이체행 회색 강조, **시트 보호**(입력칸 A~H만 편집 가능, 수식·헬퍼 잠금).
- 진단: 룰 기반 동적 텍스트(예: 저축률 ≥20% 🟢 / 10~20% 🟡 / <10% 🔴).
- 기준 연/월을 `설정`에서 바꾸면 전 시트가 그 기간으로 갱신.

### 2-5. 기술 노트 (Windows 환경 — 재현 시 주의)
- 스킬 제공 `recalc.py`는 **Windows에서 `AF_UNIX` 미지원으로 작동 안 함**. 대신 **Excel COM(win32com)** 으로 재계산·검사 (`verify_and_preview.py`).
- `ISOWEEKNUM` 등 Excel 2013+ 함수는 openpyxl에서 `#NAME?` 유발 → **`WEEKNUM(date,2)`** 로 대체함.
- 차트 막대별 색(dataPoint)은 **시트 전체 CopyPicture 캡처에선 단색으로 뭉개져 보일 수 있음**(실제 파일엔 정상). 차트 단독 검증은 `ChartObject.Chart.Export` 사용. 단일 계열 막대 다색은 `varyColors=1` + dPt 둘 다 필요.
- 한글 파일명·경로 정상 동작. 빌드 전 열린 Excel은 파일 잠금 유발 → 재빌드 시 Excel 프로세스 종료 후 진행.

---

## 3. 웹앱 전환 계획 (다음 단계 — 메인)

> 핵심 전략: **이 엑셀을 살아있는 명세서(MVP 사양)로 삼는다.** 데이터 구조·예산 룰·진단 로직이 이미 검증됨.

### 3-1. 데이터 모델 (제안 스키마 — 엑셀 ↔ DB 대응)
| 테이블 | 주요 컬럼 |
|---|---|
| `transactions` | date, type(수입/지출/이체), category_id, subcategory, member_id, payment_method_id, amount(정수·원), memo |
| `categories` | name, kind(고정/변동), type(수입/지출), monthly_budget |
| `members` | name |
| `payment_methods` | name |
| `households` / `users` | 가구 단위 멀티유저 + 인증 |

### 3-2. 기술 스택 (웹 우선)
| 영역 | 선택 | 이유 |
|---|---|---|
| 프론트 | Next.js + TypeScript + Tailwind | 반응형, 대시보드 빠른 구현 |
| 차트 | Recharts 또는 Chart.js | 엑셀 차트 4종 이식 |
| 백엔드/DB | Supabase (Postgres + Auth + Realtime) | 가족 실시간 공유·인증 빠르게 |
| 배포 | Vercel + Supabase | 무료 티어로 MVP 가능 |
| 모바일 | 반응형 + PWA | 설치형·오프라인 입력 |

### 3-3. 기능 우선순위 & 로드맵
| 단계 | 범위 | 기간(예상) |
|---|---|---|
| Phase 0 — 설계 | 엑셀을 명세로 화면/스키마 확정, 와이어프레임 | ~1주 |
| Phase 1 — MVP | 거래 CRUD, 카테고리/예산 설정, 월간 대시보드, 가족 공유, **CSV(엑셀) 가져오기** | 3~4주 |
| Phase 2 — 리포트/진단 | 주·분기·연 리포트, 자동 진단·인사이트, 예산 알림 | ~3주 |
| Phase 3 — 자동화 | 반복거래, 영수증 OCR, 푸시 알림 | ~4주 |
| Phase 4 — 확장 | 저축 목표, 은행/카드 연동, 모바일 네이티브 | 별도 검토 |

### 3-4. 엑셀 → 앱 마이그레이션
- `거래내역`을 CSV로 내보내 앱의 가져오기 기능으로 적재. 카테고리·예산도 동일.

---

## 4. 구현 시 사이드이펙트 & 검토사항 (미리 정해둘 것)

| 항목 | 사이드이펙트 | 대응 |
|---|---|---|
| 카테고리 수정/삭제 | 과거 거래 분류 깨짐 | FK + soft delete, 표시명 분리 |
| 예산 변경 | 지난달 집행률까지 바뀜 | 예산을 **월별 스냅샷**으로 저장 |
| 이체/저축 | 수입+지출 이중계상 | `transfer` 타입은 집계 제외 |
| 할부·구독 | 그 달만 폭증 | 분할 스케줄 + 반복거래 엔진 |
| 주차·월 경계 | 집계 불일치 | 회계월 시작일 설정, 주 정의 고정 |
| 거래 수만 건 | 대시보드 지연 | 사전집계(materialized view)·인덱스 |
| 가족 공유 | 남의 집 데이터 노출 위험 | **Supabase RLS(행 보안) 필수**, 권한 역할, 동시편집 충돌, 가족 내 private 플래그 |
| 입력 UX | "귀찮아서 안 씀" = 가계부 실패 1순위 | 2탭 입력, 자동분류 추천, 오프라인 동기화 |
| 은행/카드 연동 | **마이데이터 라이선스 필요**(개인 직접 불가) | 초기엔 수동입력 + CSV, 연동은 후순위(제휴/스크래핑 API) |
| 금액 정밀도 | 부동소수 오차 | 정수(최소단위) 저장 |
| 진단 코칭 | 잔소리처럼 느껴지면 역효과 | 톤·빈도 설계 |
| 법무 | 개인정보 | 처리방침·약관, 탈퇴 시 파기, 데이터 export, 로그 마스킹 |

**먼저 정할 결정**: ① 회계월 시작일(1일 vs 급여일) ② 저축/이체를 지출로 볼지 ③ 권한 역할 ④ 가족 내 프라이버시 허용 ⑤ 자동연동 시점(=수동/CSV로 시작) ⑥ 데이터 파기·export 정책

---

## 5. 다음 액션 (이어서 할 일 — 골라서 시작)
- **(a)** DB 스키마 SQL 작성 (Supabase용, 3-1 기반)
- **(b)** MVP 화면 와이어프레임 (입력 화면 + 월간 대시보드)
- **(c)** Next.js 프로젝트 스캐폴딩 + 월간 대시보드 1화면 프로토타입 (2-3 색상 그대로 적용)
- **(d)** 엑셀 카테고리·예산을 실제 우리 집 기준으로 조정 (웹앱 전 데이터 정리)

---

## 6. 관련 파일 목록
| 파일 | 위치 | 역할 |
|---|---|---|
| 우리집_가계부.xlsx | `C:\Users\Osstem\` | 완성된 가계부(결과물) |
| build_budget.py | `C:\Users\Osstem\` | 엑셀 재생성 스크립트(openpyxl) |
| verify_and_preview.py | `C:\Users\Osstem\` | 검증·PNG 미리보기(Excel COM) |
| 가계부_프로젝트_핸드오프.md | `C:\Users\Osstem\` | (이 문서) |

> 이어받는 분께: 엑셀 디자인/기능을 바꾸려면 `build_budget.py` 수정 → 실행 → `verify_and_preview.py`로 확인. 웹앱은 2-1~2-4(명세)와 3장(계획)을 기반으로 Phase 0부터 시작하면 됩니다.
